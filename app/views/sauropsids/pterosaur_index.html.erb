<body class="ptero_index">

	<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	  <div class="container-fluid">
	    <!-- Brand and toggle get grouped for better mobile display -->
	    <div class="navbar-header">
	      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
	        <span class="sr-only">Toggle navigation</span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	      </button>
	      <%= link_to 'Home', sauropsids_path, :class => "navbar-brand saur_bar" %>
	    </div>

	    <!-- Collect the nav links, forms, and other content for toggling -->
	    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
	      <ul class="nav navbar-nav">
	        <%= link_to 'Pterosaurs', pterosaur_index_path, :class => "navbar-brand saur_bar" %>
	        <%= link_to 'Ichthyosaurs', ichthyosaur_index_path, :class => "navbar-brand saur_bar" %>
	      </ul>
	      <ul class="nav navbar-nav navbar-right">
	        <li>	
				<div class="user_header">
					<% if current_user %>
					  <%= current_user.email %>
					  <%= link_to "Log Out", logout_path, :class => "user_header" %>
					<% else %>
					  <%= link_to "Sign Up", signup_path, :class => "user_header" %> or
					  <%= link_to "Log In", login_path, :class => "user_header" %>
					<% end %>
				</div>
				<% flash.each do |name,msg| %>
					<%= content_tag :div, msg, id: "flash_#{name}" %>
				<% end %>
		</li>
	          </ul>
	        </li>
	      </ul>
	    </div><!-- /.navbar-collapse -->
	  </div><!-- /.container-fluid -->
	</nav>

	<h1 id="ptero_headline">Pterosaurs</h1>

	<div id="ptero_info">
		<ul>
			<% @sauropsids.each do |s| %>
			 	
				<li>
					<%= link_to(sauropsid_path(s)) do %>
						<strong><%= s.name %></strong><br>
					<% end %>
					<%= s.age %><br>
					<%= s.species %><br>
					<%= s.location %><br>
				</li>

			<% end %>
		</ul>
	</div>

	<%= image_tag "pteranodon_by_zdenek_burian_1960.jpg", size:"463x463", alt: "pteronadon_water", class: "img-circle" %>

</body>